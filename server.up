use std::console;
use workflows::{blender, webrender, file};
use clients;

clientList = clients.find('192.168.2.0', '192.168.2.255');

var frames = blender.split("example.blend", clientList);

String final = save.images(frames, "/output/");